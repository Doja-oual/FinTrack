<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-50">
    
    <!-- Navbar -->
    <nav class="bg-white shadow-sm border-b">
        <div class="container mx-auto px-4">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center space-x-6">
                    <a href="/dashboard" class="text-2xl font-bold bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                         FinSolutions
                    </a>
                    <div class="hidden md:flex space-x-4">
                        <a href="/dashboard" class="text-gray-600 hover:text-purple-600">Tableau de bord</a>
                        <a href="/transactions" class="text-gray-600 hover:text-purple-600">Transactions</a>
                        <a href="/budgets" class="text-purple-600 font-semibold">Budgets</a>
                        <a href="/categories" class="text-gray-600 hover:text-purple-600">Catégories</a>
                    </div>
                </div>
                <div class="flex items-center space-x-4">
                    <span class="text-gray-600">Bonjour, <%= user.firstName %></span>
                    <a href="/logout" class="text-red-600 hover:text-red-800">
                        <i class="fas fa-sign-out-alt"></i>
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="container mx-auto px-4 py-8">
        
        <!-- Header -->
        <div class="mb-8">
            <div class="flex justify-between items-center mb-6">
                <div>
                    <h1 class="text-3xl font-bold text-gray-800">Mes Budgets</h1>
                    <p class="text-gray-600 mt-1">
                        Période : 
                        <span class="font-semibold">
                            <%= new Date(currentYear, currentMonth - 1).toLocaleString('fr-FR', {month: 'long', year: 'numeric'}) %>
                        </span>
                    </p>
                </div>
                <a href="/budgets/create" class="bg-gradient-to-r from-green-500 to-teal-500 text-white font-semibold px-6 py-3 rounded-lg hover:from-green-600 hover:to-teal-600 transition shadow-lg">
                    <i class="fas fa-plus mr-2"></i>Nouveau budget
                </a>
            </div>

            <!-- Messages -->
            <% if (success) { %>
                <div class="bg-green-50 border-l-4 border-green-500 p-4 mb-4 rounded">
                    <p class="text-green-700"><i class="fas fa-check-circle mr-2"></i><%= success %></p>
                </div>
            <% } %>
            <% if (error) { %>
                <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-4 rounded">
                    <p class="text-red-700"><i class="fas fa-exclamation-circle mr-2"></i><%= error %></p>
                </div>
            <% } %>

            <!-- Sélecteur de période -->
            <div class="bg-white rounded-xl shadow p-4">
                <form method="GET" action="/budgets" class="flex items-center space-x-4">
                    <label class="font-semibold text-gray-700">Période :</label>
                    <select name="month" class="px-4 py-2 border border-gray-300 rounded-lg">
                        <% for(let m = 1; m <= 12; m++) { %>
                            <option value="<%= m %>" <%= currentMonth == m ? 'selected' : '' %>>
                                <%= new Date(2000, m-1).toLocaleString('fr-FR', {month: 'long'}) %>
                            </option>
                        <% } %>
                    </select>
                    <select name="year" class="px-4 py-2 border border-gray-300 rounded-lg">
                        <% for(let y = 2024; y <= 2030; y++) { %>
                            <option value="<%= y %>" <%= currentYear == y ? 'selected' : '' %>><%= y %></option>
                        <% } %>
                    </select>
                    <button type="submit" class="bg-purple-600 text-white font-semibold px-6 py-2 rounded-lg hover:bg-purple-700 transition">
                        <i class="fas fa-search mr-2"></i>Afficher
                    </button>
                </form>
            </div>
        </div>

        <!-- Liste des budgets -->
        <% if (budgets.length === 0) { %>
            <div class="bg-white rounded-xl shadow p-16 text-center">
                <i class="fas fa-piggy-bank text-6xl text-gray-300 mb-4"></i>
                <p class="text-gray-500 text-lg mb-2">Aucun budget pour cette période</p>
                <p class="text-gray-400 mb-6">Créez votre premier budget pour mieux contrôler vos dépenses</p>
                <a href="/budgets/create" class="inline-block bg-gradient-to-r from-green-500 to-teal-500 text-white font-semibold px-8 py-3 rounded-lg hover:from-green-600 hover:to-teal-600 transition">
                    <i class="fas fa-plus mr-2"></i>Créer un budget
                </a>
            </div>
        <% } else { %>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <% budgets.forEach(budget => { %>
                    <div class="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition">
                        <!-- Header -->
                        <div class="p-6 border-l-4" style="border-color: <%= budget.category.color %>">
                            <div class="flex items-center justify-between mb-4">
                                <div class="flex items-center">
                                    <div class="w-12 h-12 rounded-full flex items-center justify-center mr-3" style="background-color: <%= budget.category.color %>20">
                                        <i class="<%= budget.category.icon %> text-2xl" style="color: <%= budget.category.color %>"></i>
                                    </div>
                                    <div>
                                        <h3 class="font-bold text-lg text-gray-800"><%= budget.category.name %></h3>
                                        <p class="text-sm text-gray-500">Budget mensuel</p>
                                    </div>
                                </div>
                            </div>

                            <!-- Barre de progression -->
                            <div class="mb-4">
                                <div class="flex justify-between text-sm mb-2">
                                    <span class="text-gray-600">
                                        <%= budget.spent.toFixed(2) %> MAD / <%= parseFloat(budget.amount).toFixed(2) %> MAD
                                    </span>
                                    <span class="font-bold <%= budget.isOverBudget ? 'text-red-600' : budget.isNearLimit ? 'text-orange-500' : 'text-green-600' %>">
                                        <%= budget.percentage.toFixed(0) %>%
                                    </span>
                                </div>
                                <div class="w-full bg-gray-200 rounded-full h-3 overflow-hidden">
                                    <div class="h-full rounded-full transition-all duration-500 <%= budget.isOverBudget ? 'bg-red-500' : budget.isNearLimit ? 'bg-orange-500' : 'bg-green-500' %>" 
                                         style="width: <%= Math.min(100, budget.percentage) %>%">
                                    </div>
                                </div>
                            </div>

                            <!-- Statut -->
                            <div class="mb-4">
                                <% if (budget.isOverBudget) { %>
                                    <div class="bg-red-50 border border-red-200 rounded-lg p-3">
                                        <p class="text-sm text-red-700 font-semibold">
                                            <i class="fas fa-exclamation-triangle mr-2"></i>
                                            Budget dépassé de <%= (budget.spent - budget.amount).toFixed(2) %> MAD
                                        </p>
                                    </div>
                                <% } else if (budget.isNearLimit) { %>
                                    <div class="bg-orange-50 border border-orange-200 rounded-lg p-3">
                                        <p class="text-sm text-orange-700 font-semibold">
                                            <i class="fas fa-exclamation-circle mr-2"></i>
                                            Attention : <%= budget.alertThreshold %>% atteint
                                        </p>
                                    </div>
                                <% } else { %>
                                    <div class="bg-green-50 border border-green-200 rounded-lg p-3">
                                        <p class="text-sm text-green-700 font-semibold">
                                            <i class="fas fa-check-circle mr-2"></i>
                                            Reste : <%= budget.remaining.toFixed(2) %> MAD
                                        </p>
                                    </div>
                                <% } %>
                            </div>

                            <!-- Actions -->
                            <div class="flex space-x-2">
                                <a href="/budgets/<%= budget.id %>/edit" class="flex-1 bg-blue-500 text-white text-center py-2 rounded-lg hover:bg-blue-600 transition">
                                    <i class="fas fa-edit mr-1"></i>Modifier
                                </a>
                                <form method="POST" action="/budgets/<%= budget.id %>/delete" class="flex-1" onsubmit="return confirm('Supprimer ce budget ?')">
                                    <button type="submit" class="w-full bg-red-500 text-white py-2 rounded-lg hover:bg-red-600 transition">
                                        <i class="fas fa-trash mr-1"></i>Supprimer
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>

        <!-- Info Box -->
        <div class="mt-8 bg-blue-50 border-l-4 border-blue-500 p-6 rounded-lg">
            <div class="flex items-start">
                <i class="fas fa-lightbulb text-blue-500 text-2xl mr-4 mt-1"></i>
                <div>
                    <p class="font-semibold text-blue-800 mb-2">Conseils pour gérer vos budgets</p>
                    <ul class="text-sm text-blue-700 space-y-1">
                        <li>• Créez des budgets réalistes basés sur vos dépenses passées</li>
                        <li>• Ajustez le seuil d'alerte selon vos besoins (recommandé : 80%)</li>
                        <li>• Vérifiez régulièrement l'évolution de vos dépenses</li>
                        <li>• N'hésitez pas à modifier vos budgets si nécessaire</li>
                    </ul>
                </div>
            </div>
        </div>

    </div>

</body>
</html>