<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gradient-to-br from-pink-100 via-purple-100 to-blue-100 min-h-screen">
    
    <div class="container mx-auto px-4 py-8">
        <div class="max-w-2xl mx-auto">
            
            <div class="bg-white rounded-2xl shadow-xl p-8">
                <div class="flex items-center justify-between mb-6">
                    <h1 class="text-3xl font-bold text-gray-800">
                        <i class="fas fa-plus-circle text-purple-500 mr-3"></i>
                        Nouvelle Catégorie
                    </h1>
                    <a href="/categories" class="text-gray-600 hover:text-gray-800">
                        <i class="fas fa-times text-2xl"></i>
                    </a>
                </div>

                <% if (error) { %>
                    <div class="bg-red-100 border-l-4 border-red-500 p-4 mb-6 rounded">
                        <p class="text-red-700"><%= error %></p>
                    </div>
                <% } %>

                <form method="POST" action="/categories" class="space-y-6">
                    
                    <!-- Nom -->
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-2">
                            Nom de la catégorie *
                        </label>
                        <input 
                            type="text" 
                            name="name" 
                            required
                            placeholder="Ex: Restaurant, Salaire..."
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-purple-500 focus:ring-4 focus:ring-purple-100 transition outline-none"
                        >
                    </div>

                    <!-- Type -->
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-2">
                            Type *
                        </label>
                        <div class="grid grid-cols-2 gap-4">
                            <label class="flex items-center p-4 border-2 border-gray-200 rounded-xl cursor-pointer hover:border-green-400 transition">
                                <input type="radio" name="type" value="income" required class="mr-3">
                                <i class="fas fa-arrow-up text-green-500 text-xl mr-2"></i>
                                <span class="font-semibold">Revenu</span>
                            </label>
                            <label class="flex items-center p-4 border-2 border-gray-200 rounded-xl cursor-pointer hover:border-red-400 transition">
                                <input type="radio" name="type" value="expense" required class="mr-3">
                                <i class="fas fa-arrow-down text-red-500 text-xl mr-2"></i>
                                <span class="font-semibold">Dépense</span>
                            </label>
                        </div>
                    </div>

                    <!-- Couleur -->
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-2">
                            Couleur
                        </label>
                        <div class="flex items-center space-x-4">
                            <input 
                                type="color" 
                                name="color" 
                                value="#007bff"
                                class="h-12 w-24 rounded-lg cursor-pointer"
                            >
                            <span class="text-gray-600">Choisissez une couleur pour identifier facilement cette catégorie</span>
                        </div>
                    </div>

                    <!-- Icône -->
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-2">
                            Icône (Font Awesome)
                        </label>
                        <select name="icon" class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-purple-500 focus:ring-4 focus:ring-purple-100 transition outline-none">
                            <option value="fas fa-utensils">🍴 Restaurant</option>
                            <option value="fas fa-car">🚗 Transport</option>
                            <option value="fas fa-home">🏠 Logement</option>
                            <option value="fas fa-heartbeat">❤️ Santé</option>
                            <option value="fas fa-gamepad">🎮 Loisirs</option>
                            <option value="fas fa-shopping-bag">🛍️ Shopping</option>
                            <option value="fas fa-wallet">💰 Salaire</option>
                            <option value="fas fa-laptop">💻 Freelance</option>
                            <option value="fas fa-chart-line">📈 Investissements</option>
                            <option value="fas fa-tag">🏷️ Autre</option>
                        </select>
                    </div>

                    <!-- Boutons -->
                    <div class="flex space-x-4 pt-4">
                        <button 
                            type="submit" 
                            class="flex-1 bg-gradient-to-r from-purple-500 to-pink-500 text-white font-bold py-3 rounded-xl hover:from-purple-600 hover:to-pink-600 transition"
                        >
                            <i class="fas fa-save mr-2"></i>
                            Créer la catégorie
                        </button>
                        <a 
                            href="/categories" 
                            class="flex-1 bg-gray-200 text-gray-700 font-bold py-3 rounded-xl hover:bg-gray-300 transition text-center"
                        >
                            Annuler
                        </a>
                    </div>

                </form>
            </div>

        </div>
    </div>

</body>
</html>