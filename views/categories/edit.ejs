<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gradient-to-br from-pink-100 via-purple-100 to-blue-100 min-h-screen">
    
    <div class="container mx-auto px-4 py-8">
        <div class="max-w-2xl mx-auto">
            
            <div class="bg-white rounded-2xl shadow-xl p-8">
                <div class="flex items-center justify-between mb-6">
                    <h1 class="text-3xl font-bold text-gray-800">
                        <i class="fas fa-edit text-blue-500 mr-3"></i>
                        Modifier la Catégorie
                    </h1>
                    <a href="/categories" class="text-gray-600 hover:text-gray-800">
                        <i class="fas fa-times text-2xl"></i>
                    </a>
                </div>

                <% if (error) { %>
                    <div class="bg-red-100 border-l-4 border-red-500 p-4 mb-6 rounded">
                        <p class="text-red-700"><%= error %></p>
                    </div>
                <% } %>

                <form method="POST" action="/categories/<%= category.id %>" class="space-y-6">
                    
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-2">
                            Nom de la catégorie *
                        </label>
                        <input 
                            type="text" 
                            name="name" 
                            value="<%= category.name %>"
                            required
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-4 focus:ring-blue-100 transition outline-none"
                        >
                    </div>

                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-2">
                            Type *
                        </label>
                        <div class="grid grid-cols-2 gap-4">
                            <label class="flex items-center p-4 border-2 rounded-xl cursor-pointer <%= category.type === 'income' ? 'border-green-500 bg-green-50' : 'border-gray-200' %>">
                                <input type="radio" name="type" value="income" <%= category.type === 'income' ? 'checked' : '' %> required class="mr-3">
                                <i class="fas fa-arrow-up text-green-500 text-xl mr-2"></i>
                                <span class="font-semibold">Revenu</span>
                            </label>
                            <label class="flex items-center p-4 border-2 rounded-xl cursor-pointer <%= category.type === 'expense' ? 'border-red-500 bg-red-50' : 'border-gray-200' %>">
                                <input type="radio" name="type" value="expense" <%= category.type === 'expense' ? 'checked' : '' %> required class="mr-3">
                                <i class="fas fa-arrow-down text-red-500 text-xl mr-2"></i>
                                <span class="font-semibold">Dépense</span>
                            </label>
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-2">
                            Couleur
                        </label>
                        <input 
                            type="color" 
                            name="color" 
                            value="<%= category.color %>"
                            class="h-12 w-24 rounded-lg cursor-pointer"
                        >
                    </div>

                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-2">
                            Icône
                        </label>
                        <input 
                            type="text" 
                            name="icon" 
                            value="<%= category.icon %>"
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 transition outline-none"
                        >
                    </div>

                    <div class="flex space-x-4 pt-4">
                        <button 
                            type="submit" 
                            class="flex-1 bg-blue-500 text-white font-bold py-3 rounded-xl hover:bg-blue-600 transition"
                        >
                            <i class="fas fa-save mr-2"></i>
                            Enregistrer
                        </button>
                        <a 
                            href="/categories" 
                            class="flex-1 bg-gray-200 text-gray-700 font-bold py-3 rounded-xl hover:bg-gray-300 transition text-center"
                        >
                            Annuler
                        </a>
                    </div>

                </form>
            </div>

        </div>
    </div>

</body>
</html>