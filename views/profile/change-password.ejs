<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - FinSolutions</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gradient-to-br from-pink-50 via-purple-50 to-rose-50 min-h-screen">
    <div class="container mx-auto px-6 py-8 max-w-2xl">
        <a href="/dashboard" class="inline-flex items-center text-gray-600 hover:text-pink-500 mb-6">
            <i class="fas fa-arrow-left mr-2"></i>
            Retour au dashboard
        </a>

        <div class="bg-white rounded-3xl shadow-xl p-8 border-2 border-purple-100">
            <div class="text-center mb-6">
                <div class="inline-block bg-gradient-to-br from-purple-400 to-indigo-400 rounded-full p-6 mb-4">
                    <i class="fas fa-lock text-white text-4xl"></i>
                </div>
                <h1 class="text-3xl font-bold text-gray-800"><%= title %></h1>
                <p class="text-gray-500">Sécurisez votre compte</p>
            </div>

            <% if (error) { %>
                <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-6 rounded-lg">
                    <p class="text-red-700"><i class="fas fa-exclamation-circle mr-2"></i><%= error %></p>
                </div>
            <% } %>

            <% if (success) { %>
                <div class="bg-green-50 border-l-4 border-green-500 p-4 mb-6 rounded-lg">
                    <p class="text-green-700"><i class="fas fa-check-circle mr-2"></i><%= success %></p>
                </div>
            <% } %>

            <form method="POST" action="/profile/change-password">
                <div class="space-y-4">
                    <div>
                        <label class="block text-gray-700 font-medium mb-2">
                            <i class="fas fa-key mr-2 text-gray-400"></i>Mot de passe actuel
                        </label>
                        <input 
                            type="password" 
                            name="currentPassword" 
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-purple-400 focus:outline-none transition"
                            required
                        >
                    </div>

                    <div>
                        <label class="block text-gray-700 font-medium mb-2">
                            <i class="fas fa-lock mr-2 text-purple-400"></i>Nouveau mot de passe
                        </label>
                        <input 
                            type="password" 
                            name="newPassword" 
                            minlength="6"
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-purple-400 focus:outline-none transition"
                            required
                        >
                        <p class="text-xs text-gray-500 mt-1">Minimum 6 caractères</p>
                    </div>

                    <div>
                        <label class="block text-gray-700 font-medium mb-2">
                            <i class="fas fa-check-circle mr-2 text-green-400"></i>Confirmer le nouveau mot de passe
                        </label>
                        <input 
                            type="password" 
                            name="confirmPassword" 
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-green-400 focus:outline-none transition"
                            required
                        >
                    </div>

                    <div class="pt-4">
                        <button 
                            type="submit"
                            class="w-full bg-gradient-to-r from-purple-400 to-indigo-400 text-white py-4 rounded-xl font-bold hover:shadow-lg transition transform hover:-translate-y-1"
                        >
                            <i class="fas fa-shield-alt mr-2"></i>
                            Changer le mot de passe
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</body>
</html>