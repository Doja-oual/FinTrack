<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - FinSolutions</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gradient-to-br from-pink-50 via-purple-50 to-rose-50 min-h-screen">
    <div class="container mx-auto px-6 py-8 max-w-2xl">
        <a href="/savings-goals" class="inline-flex items-center text-gray-600 hover:text-pink-500 mb-6">
            <i class="fas fa-arrow-left mr-2"></i>Retour aux objectifs
        </a>

        <div class="bg-white rounded-3xl shadow-xl p-8 border-2 border-pink-100">
            <h1 class="text-3xl font-bold text-gray-800 mb-6"><%= title %></h1>

            <% if (error) { %>
                <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-6 rounded-lg">
                    <p class="text-red-700"><%= error %></p>
                </div>
            <% } %>

            <form method="POST" action="/savings-goals">
                <div class="space-y-4">
                    <div>
                        <label class="block text-gray-700 font-medium mb-2">Nom de l'objectif *</label>
                        <input type="text" name="name" required
                               placeholder="Ex: Vacances 2026"
                               class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-pink-400 focus:outline-none">
                    </div>

                    <div>
                        <label class="block text-gray-700 font-medium mb-2">Montant cible (MAD) *</label>
                        <input type="number" name="targetAmount" step="0.01" min="0.01" required
                               placeholder="10000"
                               class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-pink-400 focus:outline-none">
                    </div>

                    <div>
                        <label class="block text-gray-700 font-medium mb-2">Date limite</label>
                        <input type="date" name="targetDate"
                               class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-pink-400 focus:outline-none">
                    </div>

                    <div>
                        <label class="block text-gray-700 font-medium mb-2">Description</label>
                        <textarea name="description" rows="3"
                                  placeholder="Décrivez votre objectif..."
                                  class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-pink-400 focus:outline-none"></textarea>
                    </div>

                    <button type="submit" 
                            class="w-full bg-gradient-to-r from-pink-400 to-purple-400 text-white py-4 rounded-xl font-bold hover:shadow-lg transition">
                        <i class="fas fa-save mr-2"></i>Créer l'objectif
                    </button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>